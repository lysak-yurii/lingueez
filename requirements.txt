# GUI Framework
PySide6>=6.6.0

# AI providers
openai>=1.0.0
google-genai>=1.0.0

# HTTP Requests
requests>=2.28.0

# Data Processing
pandas>=1.5.0
numpy>=1.23.0

# Excel Support
openpyxl>=3.0.0

# PDF Generation
reportlab>=3.6.0

# Text sources (article extraction, RSS feeds)
trafilatura>=1.8.0
feedparser>=6.0.0

# Cloud Database
supabase>=2.0.0

# Environment Variables
python-dotenv>=1.0.0

# Secure credential storage (auth session tokens)
keyring>=24.0.0          # OS keychain; falls back to an encrypted local file
cryptography>=41.0.0     # encrypts the local session fallback when no keychain

# System Integration
keyboard>=0.13.5; sys_platform == "win32"
pynput>=1.7.0

# Audio Processing
pygame>=2.1.0
gtts>=2.3.0
pydub>=0.25.0
# pydub imports the stdlib `audioop` module, which PEP 594 removed in Python 3.13.
# The audioop-lts backport restores it; only needed on 3.13+ (the Flatpak runtime).
audioop-lts>=0.2.2; python_version >= "3.13"
google-cloud-texttospeech>=2.14.0
