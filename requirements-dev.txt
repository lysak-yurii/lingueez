# Development / CI-only tooling. Not needed to run the app.
# Install with:  pip install -r requirements-dev.txt
#
# Tests use the standard library (unittest), so nothing extra is required to
# run them — only the linter and the site tooling live here.
ruff==0.6.9

# tools/site/preview.py — renders docs/ without Ruby/Jekyll, for local preview.
PyYAML
Markdown

# tools/site/build_hero.py — composes the landing-page hero.
cairosvg
Pillow
